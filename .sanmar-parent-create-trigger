# Pilot: create net-new SanMar styles NetSuite doesn't carry -- parent matrix
# item via REST, then children via the matrix RESTlet (parent-before-child).
# Refreshes the net-new list via create-preview, then creates up to MAX_STYLES.
# DRY first: reports which styles + how many children + which options it would
# create, no writes. Flip DRY_RUN=false for the live pilot (needs the matrix
# RESTlet redeployed with the upccode line).
# Dry pilot was clean: 5 styles / ~16 children / 2 new colours (Petrol, Wasabe),
# 0 failures. RESTlet re-uploaded with the upccode line. GO LIVE on the 5-style
# pilot -- creates the parents (REST) + children (RESTlet) for real.
# run: parent-create-LIVE-pilot-2026-07-21
DRY_RUN=false
MAX_STYLES=5
