# Back-fill run parameters (read by sanmar-backfill.yml on push)
# POST-REFRESH rebuild: dry run against the fresh production copy.
DRY_RUN=true
STYLE_LIMIT=0
MAX_ITEMS=25
SET_EXTERNAL_IDS=false
RENAME_COLORS=true
