# Back-fill run parameters (read by sanmar-backfill.yml on push)
# RUN 2 of 3: LIVE smoke test (user-approved) — 25 UPCs in the SANDBOX,
# color renames off for this run.
DRY_RUN=false
STYLE_LIMIT=200
MAX_ITEMS=25
SET_EXTERNAL_IDS=false
RENAME_COLORS=false
