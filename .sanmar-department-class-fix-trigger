# LIVE smoke (200-item cap): first real write for this correction script.
# The dry run validated the diff logic (0 failures) but never wrote; this
# confirms the classification/fullname-resolved Class ids are actually
# accepted on a real PATCH before uncapping to the full catalog. Sequential
# with the other live writers -- never run two write workflows at once
# (concurrent writes were what caused the earlier throttling storm).
# run: department-class-fix-LIVE-smoke-2026-07-23
DRY_RUN=false
MAX_ITEMS=200
