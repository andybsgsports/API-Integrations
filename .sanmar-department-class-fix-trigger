# FULL live run: the 200-cap smoke wrote 200 with 0 failures and confirmed the
# classification/fullname-resolved Class ids land on real PATCHes. Uncapping to
# correct Department/Class across every SanMar-keyed item. Diff-aware (only
# ~thousands actually change; the rest are already correct). Sequential with the
# other live writers -- never two write workflows at once.
# run: department-class-fix-LIVE-full-2026-07-23
DRY_RUN=false
MAX_ITEMS=0
