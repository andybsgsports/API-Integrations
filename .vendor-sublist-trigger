# Re-validate Preferred Vendor ranking with a dry run first -- last real run
# was July 15 (8+ days stale: new items created since then via the SanMar/S&S/
# Momentec pipelines have never had this pass applied). Small cap first to
# confirm the account has recovered from the OAuth1/throttling issues before
# scaling back up to a full live pass.
# run: vendor-sublist-dryrun-2026-07-23
DRY_RUN=true
MAX_ITEMS=25
