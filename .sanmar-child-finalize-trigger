# Copy Department / Class / Purchase Description / Sales Description from each
# SanMar matrix parent down onto its blank children (the ones the CSV create-
# import left empty). Fill-blanks-only, SanMar children only, diff-aware.
# Dry run passed clean (0 failures; sets department, class, salesDescription,
# purchaseDescription, location per child). First LIVE run is capped at 50 to
# confirm real writes land, then we uncap to 0.
# run: child-finalize-LIVE-cap50-2026-07-20
DRY_RUN=false
MAX_ITEMS=50
