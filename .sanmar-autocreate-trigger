# Nightly SanMar auto-create (06:00 UTC): create-preview -> ensure-values ->
# RESTlet import -> child-finalize. Scheduled runs read AUTOCREATE_LIVE below.
#
# START DRY: AUTOCREATE_LIVE=false makes every run report only (no colours
# created, no import) so you can watch a full cycle first. Flip to true when
# you're ready for it to actually create new items each night.
#
# Bumping this file also fires one run immediately (with the flag below).
# run: autocreate-dry-cycle-2026-07-21
AUTOCREATE_LIVE=false
