# LIVE, now with bounded-concurrency writes (NETSUITE_WRITE_CONCURRENCY, default
# 5) so the ~28K-record store/stock/cost migration finishes in a fraction of the
# time instead of the 6h+ sequential crawl. Diff-aware, so this run only writes
# whatever the cancelled run #38 didn't already complete. Applies Store Display
# Name + Store/Stock Description + case-price cost + On Sale flag.
# run: field-update-concurrent-LIVE-2026-07-22
DRY_RUN=false
MAX_ITEMS=0
