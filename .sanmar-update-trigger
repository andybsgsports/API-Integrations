# DRY run of the new CASE-price cost basis. Reads only -- shows how many items'
# Purchase Price would change now that cost = case price (sanmar.com "Original
# Price") instead of the single-piece price, plus the on-sale count under the
# case-level sale price. Review the "price/cost/weight updated" count, then flip
# DRY_RUN=false to apply (or let the 10:00 UTC nightly run it live).
# Now also covers Store Display Name (= clean product name, polished to match
# the Display Name the description-update job sets) and Store + Stock Description
# (= the marketing copy). Watch "store name/desc updated: N".
# LIVE: apply Store Display Name (= clean name) + Store & Stock Description
# (= marketing copy), alongside the case-price cost + On Sale flag. Dry run was
# clean (0 failures).
# run: field-update-store-stock-LIVE-2026-07-22
DRY_RUN=false
MAX_ITEMS=0
