# LIVE image backfill including the new USB/TCK part-keyed coverage, so every
# DC OneSource vendor now feeds the Item Image. Diff-aware (only fills empty
# images), so this pass mainly adds USB/TCK + anything missed. Sandbox-gated.
# run: images-live-usb-tck-2026-07-20
DRY_RUN=false
MAX_ITEMS=0
