# LIVE full image backfill: apply Champro per-color images (UPC-matched) + DC
# OneSource feed images + any missing SanMar/Momentec/S&S images to the main
# Item Image. Diff-aware (only fills items with an empty image). Sandbox-gated.
# run: images-live-2026-07-20
DRY_RUN=false
MAX_ITEMS=0
