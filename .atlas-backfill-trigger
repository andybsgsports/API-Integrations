# LIVE image backfill after the Champro CSV UPC fill: the 66 grouped/no-UPC
# Champro items (incl. B041) now carry UPCs, so the champro_images.csv UPC->image
# source can finally match them and set custitem_atlas_item_image. Diff-aware
# (only fills empty images). Sandbox-gated.
# run: images-live-champro-upc-2026-07-20
DRY_RUN=false
MAX_ITEMS=0
