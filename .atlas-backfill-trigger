# Trigger file for atlas-image-backfill.yml push-trigger runs.
# Dry test of the full image wiring: Champro per-color images (UPC-matched from
# data/champro_images.csv) preferred over the DC OneSource feed primaryImageUrl.
# run: champro-upc-images-dry-2026-07-20
DRY_RUN=true
MAX_ITEMS=25
