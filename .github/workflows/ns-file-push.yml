name: NetSuite File Cabinet Push (deploys storefront code)

# Pushes edited files under suitescript/store/ back into the File Cabinet via
# SOAP update -- this DEPLOYS storefront code to the NetSuite account the
# repo secrets point at (the sandbox). Each update is verified by re-fetching
# and byte-comparing; the pre-edit originals live in git history (committed by
# the fetch workflow before editing began).
#
# Explicit-list only: the `files` input names every file to push. Nothing is
# deployed implicitly.

on:
  workflow_dispatch:
    inputs:
      files:
        description: "Comma-separated paths relative to the src_dir to push"
        required: true
      script_id:
        description: "Script record internal id anchoring the target folder"
        required: false
        default: "2358"
      src_dir:
        description: "Repo dir the paths are relative to (suitescript/store or suitescript)"
        required: false
        default: "suitescript/store"

permissions:
  contents: read

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Write .env from repo secrets
        run: |
          cat > .env <<EOF2
          NETSUITE_ACCOUNT_ID=${{ secrets.NETSUITE_ACCOUNT_ID }}
          NETSUITE_CONSUMER_KEY=${{ secrets.NETSUITE_CONSUMER_KEY }}
          NETSUITE_CONSUMER_SECRET=${{ secrets.NETSUITE_CONSUMER_SECRET }}
          NETSUITE_TOKEN_ID=${{ secrets.NETSUITE_TOKEN_ID }}
          NETSUITE_TOKEN_SECRET=${{ secrets.NETSUITE_TOKEN_SECRET }}
          SYNC_DRY_RUN=true
          NETSUITE_ALLOW_PRODUCTION_WRITES=false
          EOF2

      - name: Push files to File Cabinet
        env:
          # "matrix" resolves to the BSG matrix RESTlet's script id from
          # secrets, so its source can be pushed without exposing the id.
          FILE_PUSH_SCRIPT_ID: ${{ github.event.inputs.script_id == 'matrix' && secrets.NETSUITE_MATRIX_SCRIPT_ID || github.event.inputs.script_id }}
          FILE_PUSH_FILES: ${{ github.event.inputs.files }}
          FILE_PUSH_SRC: ${{ github.event.inputs.src_dir }}
        run: python scripts/ns_file_push.py
