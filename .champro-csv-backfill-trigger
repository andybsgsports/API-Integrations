# Champro CSV back-fill: fill UPC/GTIN, weight, keys, doc links from
# data/champro_products.csv by vendor code (SKU). Dry run first to verify
# matches (esp. B041). No inventory. DRY_RUN=false to write.
DRY_RUN=true
MAX_ITEMS=0
