# Verify the write-phase SELECT runs clean after confirming all custitem_mtec_*
# fields exist (audit: have=106) + the resilience guard. Dry run: exercises
# every SELECT chunk (the operation that was 400ing) without writing.
DRY_RUN=true
MAX_ITEMS=0
