# LIVE full run to apply the S&S MAP fix: 0.01 "no MAP" placeholders are now
# treated as no-MAP, so this pass blanks every stale 0.01 custitem_ss_map and
# writes real MAPs (>1c) where S&S actually restricts. No cap so it reaches the
# whole matched catalog. Sandbox-gated.
# run: ss-map-sentinel-fix-2026-07-20
DRY_RUN=false
MAX_ITEMS=0
